// ScrollReveal configuration
// Currently commented out but kept for reference
// "use client";
// import ScrollReveal from "scrollreveal";

// const isSSR = typeof window === "undefined";
// const sr = isSSR ? null : ScrollReveal();

// export default sr;
